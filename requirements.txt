pytesseract>=0.3,<0.4
# renderer fallback for the run-13 full-page image-ownership passes:
# pdftoppm comes from poppler-utils in the Docker image (primary renderer),
# so PyMuPDF is NOT a hard build dependency -- it is an optional fallback
# for sandbox/Colab where poppler is absent. DO NOT put it in requirements:
# a missing/compiling wheel must never fail a Railway build. Local use:
# pip install pymupdf
